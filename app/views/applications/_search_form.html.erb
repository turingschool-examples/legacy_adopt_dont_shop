<%= form_with url: "/applications/#{@application.id}", method: :get, local: true do |form| %>
  <%= form.label :search_pets_by_name  %>
  <%= form.text_field :pet_search %>
  <%= form.submit 'Submit' %>
<% end %>
