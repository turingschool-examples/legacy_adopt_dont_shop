<h1>Applications</h1>
<%= link_to 'Start an Application', applications_new_path%><br>

<% @applications.each do |application| %> <br>
<%= link_to "#{application.name}", applications_show_path(application.id) %>
<% end  %>
