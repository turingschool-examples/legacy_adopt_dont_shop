<h2><%= @application.name %>'s Application for Adoption</h2>

<p>Name:
  <%= @application.name %><br>
  Address:
  <%= @application.street_address %>
  <%= @application.city %>
  <%= @application.state %>
  <%= @application.zip %><br>
  Description:
  <%= @application.description %><br>
  Pets:
  <%# <%= @application.pet_applications.pets.each do |pet|
       pet.name
      end %></p>
<%= form_tag("/pets", method: :get) do %>
<%= label_tag :add_a_pet_to_this_application %>
<%= search_field_tag :search, params[:search] %>
<%= submit_tag "Search", name: nil %>
<% end %>
<h3>Application Status: <%= @application.status %></h3>
